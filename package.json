{
  "name": "jetty",
  "private": true,
  "workspaces": [
    "shared",
    "server"
  ],
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "typecheck": "tsc --noEmit -p shared && tsc --noEmit -p server",
    "test": "bun test",
    "dev:server": "bun run --watch server/src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "typescript": "^7.0.2"
  }
}
